# Grid specs

Quadrature choices for the XC integral. `becke` is the default atom-centered
grid; `points` wraps an explicit grid; a `chunk` on either streams the XC
integral in O(chunk·nao) memory.

::: dftax.grid.becke
::: dftax.grid.points
::: dftax.grid.becke_grid
