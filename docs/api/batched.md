# Batched evaluation

One `vmap`ped solve over a stack of geometries. Orbital-sized outputs are
opt-in; `mesh=` shards the batch axis across devices.

::: dftax.ks.batched.scf_batched
::: dftax.ks.batched.BatchedResult
